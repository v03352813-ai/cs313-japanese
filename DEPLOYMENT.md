# CS313.CN 韩语及多语种平台 · 0 成本极速部署与上线指南

本指南指导您如何将项目零成本、高可用地部署到 **Cloudflare Pages / Vercel**，并绑定您的独立域名 **`kr.cs313.cn`**，配合**闲管家**实现 24 小时无人值守自动发货。

---

## 目录
1. [第一步：前端打包与上线 (Cloudflare Pages)](#第一步前端打包与上线)
2. [第二步：绑定二级域名 `kr.cs313.cn`](#第二步绑定二级域名-krcs313cn)
3. [第三步：部署 Serverless 鉴权中枢 (Cloudflare Worker)](#第三步部署-serverless-鉴权中枢)
4. [第四步：闲管家自动发货配置 SOP](#第四步闲管家自动发货配置-sop)
5. [常见问题与安全保障](#常见问题与安全保障)

---

## 第一步：前端打包与上线

项目已配置为极速静态打包，生成体积仅约 100KB。

### 1. 本地打包
在本地终端 `cs313-korean` 目录下执行：
```bash
npm run build
```
打包完成后会在根目录下生成 `dist/` 文件夹。

### 2. 部署到 Cloudflare Pages（全球免费加速 · 0 服务器成本）
1. 登录 [Cloudflare 控制台](https://dash.cloudflare.com/)。
2. 左侧菜单点击 **Workers 和 Pages** ➡️ 点击 **创建应用程序** ➡️ 选择 **Pages**。
3. 选择 **直接上传 (Direct Upload)**：
   * 项目名称填写：`cs313-korean`
   * 将本地刚刚生成的 **`dist` 文件夹** 直接拖拽上传。
4. 点击 **部署站点**。
   * 10 秒内即可获得一个可全球访问的免费网址（如 `cs313-korean.pages.dev`）。

---

## 第二步：绑定二级域名 `kr.cs313.cn`

在 Cloudflare Pages 项目管理页面中：

1. 点击 **自定义域 (Custom Domains)** 选项卡 ➡️ 点击 **设置自定义域**。
2. 输入您的专属二级域名：`kr.cs313.cn`（或 `korean.cs313.cn`）。
3. Cloudflare 会自动提示您添加一条 DNS 解析记录：
   * **类型 (Type)**：`CNAME`
   * **名称 (Name)**：`kr`
   * **内容 (Target)**：`cs313-korean.pages.dev`
   * **代理状态**：已代理 (橙色云朵开启，享受免费 CDN 加速与 DDoS 防护)
4. 点击确定，Cloudflare 会在 1~2 分钟内自动签发 **免费 HTTPS SSL 证书**。

> 💡 **隔离说明**：此操作仅新增一条 `kr` 子域名解析记录，**绝对不会影响您在 `cs313.cn` 上的任何现有虚拟页面与业务**。

---

## 第三步：部署 Serverless 鉴权中枢 (Cloudflare Worker)

用于处理 **2 台设备指纹绑定** 与 **防暴力破解拦截**。

1. 在 Cloudflare 控制台点击 **Workers 和 Pages** ➡️ 点击 **创建 Worker**。
2. 命名为 `cs313-auth-api`，点击部署。
3. 点击 **快速编辑 (Quick Edit)**：
   * 将本地 `serverless/worker.js` 中的全部代码复制并替换编辑器内容。
   * 点击 **保存并部署**。
4. (可选进阶) 在左侧 **D1 数据库** 创建名为 `cs313_db` 的数据库，将 `serverless/schema.sql` 中的建表语句在控制台执行即可。

---

## 第四步：闲管家自动发货配置 SOP

配合闲管家在小红书、闲鱼、淘宝实现 24 小时无人值守发货：

### 1. 生成并导出卡密
1. 打开部署好的网站（或本地 `http://localhost:5173`）。
2. 点击右上角 **设置图标 (店主后台)**。
3. 选择卡密类型（如 `韩语单语种终身卡 ¥48.8`），选择数量（如 `100 条`），点击 **立即生成**。
4. 点击 **“一键复制 (闲管家格式)”**。

### 2. 导入闲管家
1. 登录闲管家后台 ➡️ 进入 **【虚拟商品管理】** ➡️ 找到对应商品。
2. 点击 **【卡密库管理】** ➡️ **【批量导入】** ➡️ 直接粘贴剪贴板内容。

### 3. 设置自动发货私信模板
在闲管家的自动回复模板中设置为：
```text
🎉 恭喜您成功获取【CS313 韩语高效研习社 · 终身学习库】！

👉 专属学习网址：https://kr.cs313.cn
🔑 您的终身激活码：{卡密}

📌 使用须知：
1. 首次打开网站点击右上角「卡密激活」输入即可畅享 5000+ 词库与历年真题。
2. 本授权已开启安全保护，支持 1台 iPad + 1台手机/电脑 同时使用，严禁转借他人。
祝您学习愉快，考级顺利通过！🌸
```

---

## 第五步：后期扩充多语种（日/俄/西）

当韩语跑通出单后，扩充新语种极为简便：
1. 复制一份语种数据源（如 `src/data/japanese/`）。
2. 在 Cloudflare Pages 新增自定义域绑定 `jp.cs313.cn` / `es.cs313.cn` / `ru.cs313.cn`。
3. 闲管家分别上架日语/西语/俄语商品，矩阵号批量引流变现！
